<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recepies dz</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: sans-serif;
            font-size: 1rem;
        }

        .wrapper {
            max-width: 1200px;
            margin: 10px auto;
        }

        .flex {
            display: flex;
        }

        .flex-col {
            flex-direction: column;
        }

        a {
            text-decoration: none;
            color: #000;
        }

        input {
            outline: none;
            border: none;
            width: 100%;
            height: 100%;
        }

        textarea {
            outline: none;
            border: none;
            width: 100%;
            height: 100%;
        }

        select {
            border-radius: .7rem;
            border: 1px solid #e0e0e0;
            padding: .8rem .9rem;
            cursor: pointer;
            outline: none;
        }

        .input {
            border-radius: .7rem;
            border: 1px solid #e0e0e0;
            padding: .8rem .9rem;
            cursor: pointer;
        }

        button {
            background: #11c920;
            color: #fff;
            border-radius: .7rem;
            outline: none;
            border: none;
            padding: .8rem 1rem;
            cursor: pointer;
            transition: .3s background;
        }

        a.btn {
            /* background: #11c920; */
            border: 1px solid #11c920;
            background: transparent;
            border-radius: .7rem;
            color: #11c920;
            outline: none;
            padding: .8rem 1rem;
            cursor: pointer;
            transition: .3s background;
        }

        button:hover {
            background: #11c920bd;
            transition: .3s background;
        }

        .mt-1 {
            margin-top: 1rem;
        }
        
        .block {
            border: 1px solid #e0e0e0;
            border-radius: .7rem;
            padding: .8rem 1rem;
            display: flex;
            gap: 1.1rem;
            width: 100%;
        }

        .block img {
            border-radius: .3rem;
            width: 200px;
            max-height: 150px;
            object-fit: cover;
        }

        .font-title {
            font-size: 1.1rem;
            font-weight: bold;
        } 

        .font-middle {
            font-size: .9rem;
        } 

        span {
            font-weight: bold;
        }

        .grid {
            display: grid;
            grid-template-columns: 1fr 4fr;
            gap: 1rem;
        }
        .block-right {
            width: 100%;
        }

        .limit {
            max-height: 2.1rem; 
            overflow: hidden; 
            text-overflow: ellipsis; 
            display: -webkit-box; 
            -webkit-line-clamp: 3; 
            -webkit-box-orient: vertical; 
            white-space: normal;
        }
        .list {
            background: #11c92084;
            /* border-radius: .3rem; */
            border-top-left-radius: .3rem;
            border-bottom-left-radius: .3rem;
            padding: .8rem;
            -webkit-box-shadow: -1px 8px 19px 0px rgba(34, 60, 80, 0.2);
            -moz-box-shadow: -1px 8px 19px 0px rgba(34, 60, 80, 0.2);
            box-shadow: -1px 8px 19px 0px rgba(34, 60, 80, 0.2);
        }

        .choose {
            background: #e0e0e0bd;
            border-radius: .3rem;
            padding: 1rem;
            width: 100%;
            transition: .3s background;
        }

        .choose:hover {
            background: #e0e0e0;
            transition: .3s background;
        }

        .choose.active {
            background: #11c920bd;
        }

        .checkbox-hidden {
            display: none;
        }

        .checkbox-custom {
            width: 20px;
            height: 20px;
            display: inline-block;
            border: 2px solid #ccc;
            border-radius: 4px;
            background-color: #fff;
            cursor: pointer;
            position: relative;
            transition: background-color 0.3s, border-color 0.3s;
        }

        .checkbox-custom:hover {
            border-color: #007BFF;
        }

        .checkbox-hidden:checked + .checkbox-custom {
            background-color: #007BFF;
            border-color: #007BFF;
        }

        .checkbox-hidden:checked + .checkbox-custom::after {
            content: '';
            position: absolute;
            top: 4px;
            left: 6px;
            width: 6px;
            height: 12px;
            border: solid #fff;
            border-width: 0 2px 2px 0;
            transform: rotate(45deg);
        }

        .custom-checkbox label {
            display: flex;
            align-items: center;
            gap: 10px;
            cursor: pointer;
        }

    </style>
  </head>
  <body>
  <div class="wrapper">
  <div class="header flex" style="justify-content: space-between; align-items: center">
      <a href="/">RecipesBook</a>
      <a href="/recipes/new" class="btn">Добавить рецепет</a>
  </div>

    <div class="flex flex-col mt-1">
    <form method="get" action="/recipes/search">
        <div class="flex" style="gap: 1rem;">
                <div class="input" style="width: 100%;">
                    <input type="text" placeholder="Введите название рецепта" name="query">
                </div>
                <button>Найти</button>
                </div>
        </form>

     
        <%= yield %>
      
    </div>
  </body>
</html>
